rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
    
    // match /users/{user_document} {
    // 	allow read: if true;
    //   allow write: if request.auth.uid == user_document;
    // }
    
    // match /users/{user_document}/notes/{note_document} {
    // 	allow read, write: if request.auth.uid == user_document;
    // }
  }
}